MY LEARNING ROADMAP
1. Variables DONE
2. Functions
- What is a function
- Calling a function
- Receiving information in a function
- Returning information in a function DONE

3. If Statements
- Else statement DONE

4. Loops
- For Loops (for i := 0; i <…; i++)
- Looping through letters (for _, letters := range word) DONE

5. Arrays
- List of Items
- How to Read Items(using their index number)
- Accessing Items(by printing that item using its position)
-Fixed size Array(Defining numbers of items the array can carry)
-Loop + Array together( for for i := 0; i < 3; i++ {
				fmt.Println(toys[i])
			}) DONE

6. Slices
-Slices are Flexible Arrays
- How to input items in a slices
-Adding items into slice(using append)
- Creating empty slice (var name []string)
- Accessing slice item by printing their position(fmt.Println(fruits[2]) DONE

7. Maps
- Keys and values
- Looking up a value(fmt.Println(ages[“Mary])
- Creating an empty maps: age:=make(map[string]int)
-  Adding Data: (ages[“Mary”]=10)

8. Strings
- Difference between String and Rune
- Looping through a String (The loop takes each character one at a time)
-
9. Rune vs Byte
DONE

10. Errors
-  Error is a way of handling program when the program fails or ecounter a problem. But when it pass error is nil)

11. fmt Package
12. strings Package
13. os Package
14. Read Project Files
15. Build ASCII Art From Scratch


## this (fmt.Printf("%c\n", ch)) shows that show me the character, not the number value.
